spring:
  application:
    name: tokenw

  datasource:
    username: postgres
    password: postgre5432
    url: jdbc:postgresql://localhost:5432/demo
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: eneshan034@gmail.com
    password: qjbw yqcv nmcu dhbg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
